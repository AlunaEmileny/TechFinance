class Conta{
    numero : string;
    saldo: number;
    agencia: string;

    constructor(numero: string, saldo: number, agencia: string){
        this.numero = numero;
        this.saldo = saldo;
        this.agencia = agencia;
    }
}